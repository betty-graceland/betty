[project]
name = "betty"
version = "0.1.0"
description = "Betty — local-first autonomous personal business AI"
requires-python = "==3.12.*"

dependencies = [
    "betty-etl",
    "betty-claw",
]

[tool.uv.workspace]
members = ["etl", "claw"]

[tool.uv.sources]
betty-etl = { workspace = true }
betty-claw = { workspace = true }
